# CLUTCH Frontend Development Guide

## 적용 범위

이 문서는 `Clutch-FE` 저장소 전체에 적용되는 AI 개발 규칙이다. 코드 작업 전에 이 문서와 작업에 관련된 `docs/` 문서를 확인한다.

## 기준 문서

- `AGENTS.md`: AI가 반드시 따를 공통 개발 규칙
- `PRODUCT.md`: 확정된 제품 사용자, 목적과 제약
- `docs/architecture/frontend-structure.md`: 프론트엔드 폴더 구조와 계층 책임
- `design-system/clutch/MASTER.md`: UI 검색 결과를 보조 자료로 기록한 디자인 기준
- 백엔드 API와 도메인 규칙: 인접한 `Clutch-BE` 저장소의 코드와 `docs/`
- Jira: 담당자, 작업 단위와 일정
- GitHub: 소스 코드와 확정된 개발 규칙의 이력

문서와 실제 코드가 충돌하면 임의로 해석하지 않는다. 현재 작업 범위에서 코드가 확정한 계약을 확인하고 필요한 문서를 함께 갱신한다.

## 프로젝트 기술

- React 18
- Vite 5
- React Router 7 선언형 라우팅
- CSS 변수와 일반 CSS
- Spring Boot REST API 연동

## 구조 규칙

- `src/app`: 앱 진입, 라우팅과 전역 데이터 흐름
- `src/layouts`: 사용자와 관리자 앱 셸
- `src/pages`: URL 단위 화면 조립
- `src/features`: 도메인별 UI와 상태
- `src/shared`: 도메인 지식이 없는 공통 UI, 스타일과 유틸리티
- `src/api`: 공통 HTTP 처리와 도메인별 API 함수

세부 규칙은 `docs/architecture/frontend-structure.md`를 따른다.

## UI 규칙

- 사용자 메뉴는 상단에, 관리자 메뉴는 왼쪽에 배치한다.
- 공개 메뉴의 `진단` 화면과 `DebugPanel`은 별도 이슈 없이 수정하거나 이동하지 않는다.
- 기존 메인 화면은 이번 구조 확장의 시각 기준으로 사용하며 별도 요구 없이 재설계하지 않는다.
- 배팅은 매치 승리가 아니라 현재 세트의 승리 팀을 선택한다.
- 하나의 화면에서 차콜 중립색과 정해진 강조색을 일관되게 사용한다.
- 포인트와 쿠폰 행동은 금색, 블루·레드는 팀과 경기 데이터 구분에 사용한다.
- 상태는 색상과 함께 텍스트를 표시한다.
- 버튼과 링크는 키보드 포커스가 보여야 하며 주요 조작 영역은 최소 44px를 확보한다.
- 로딩, 빈 상태와 오류 상태를 화면 맥락 안에서 제공한다.
- 장식 목적의 과도한 카드, 발광 효과와 무의미한 애니메이션을 추가하지 않는다.

## 구현 규칙

- 페이지는 화면을 조립하고 URL 파라미터를 해석한다. 세부 도메인 UI는 `features`에 둔다.
- API 함수는 `src/api`의 도메인 파일에 두고 컴포넌트에서 직접 `fetch`를 작성하지 않는다.
- 공통 컴포넌트는 특정 도메인의 용어나 API 응답 구조를 알지 않아야 한다.
- 기존 컴포넌트는 이번 작업에서 실제로 수정하는 파일만 새 구조로 이동한다. 무관한 파일의 기계적 이동은 하지 않는다.
- 외부 라이브러리를 추가하기 전에 `package.json`을 확인하고 기존 구현으로 해결 가능한지 먼저 판단한다.
- 공개 API 계약을 추정하지 않는다. `Clutch-BE`의 컨트롤러와 DTO를 확인한다.
- 요구사항에 없는 기능과 상업적 수치를 임의로 만들지 않는다.

## Git 규칙

- 통합 브랜치는 `dev`이다.
- 작업 브랜치는 `dev`를 기준으로 `<type>/CLUTCH-<issue>` 형식을 사용한다.
- `main`과 `dev`에 직접 push하지 않는다.
- Jira 작업 하나당 작업 브랜치 하나를 사용한다.
- 커밋 메시지는 Conventional Commits 형식을 사용하고 한글로 작성한다.
- Pull Request 대상 브랜치는 `dev`이다.

## 검증

- 변경 후 `npm run build`를 실행한다.
- 사용자 화면과 관리자 화면을 데스크톱과 모바일 너비에서 확인한다.
- 공개 `진단` 화면과 기존 메인 화면이 계속 열리는지 확인한다.
- 테스트 또는 시각 검증을 실행하지 못하면 결과와 이유를 명시한다.
