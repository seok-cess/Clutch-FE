# CLUTCH Frontend Design System

## 시각 방향

CLUTCH는 LCK 중계 데이터와 리워드 운영을 위한 밀도 높은 다크 인터페이스다. 깊이는 큰 그림자나 발광 효과보다 차콜 표면의 명도 차, 1px 헤어라인과 정돈된 숫자 정렬로 만든다. 사용자 제공 이미지는 정보 구조와 밀도의 참고 자료이며 화면을 그대로 복제하지 않는다.

- 사용자 화면은 중계 관제 화면처럼 빠르게 훑을 수 있어야 한다.
- 관리자 화면은 장식보다 상태, 수량과 다음 작업을 우선한다.
- 블루와 레드는 경기 팀 구분에, 금색은 포인트·쿠폰과 주요 운영 행동에 제한해 사용한다.
- 상태는 색상만으로 전달하지 않고 항상 텍스트 라벨을 함께 표시한다.
- `design-system/clutch/MASTER.md`는 검색 기반 보조 자료다. 실제 구현과 충돌할 때는 이 문서, `PRODUCT.md`, CSS 토큰과 기존 CLUTCH 표현을 우선한다.

## 앱 셸과 내비게이션

### 사용자

- 데스크톱에서는 높이 `68px`의 상단 헤더에 워드마크, 주요 메뉴, 사용자 ID와 관리자 진입 링크를 배치한다.
- 메뉴 순서는 홈, 라이브, 일정, 순위, 리워드, 진단이다. 현재 위치는 블루 하단선과 밝은 텍스트로 표시한다.
- 라이브 경기가 진행 중일 때만 라이브 메뉴에 의미가 있는 레드 상태점을 표시한다.
- 헤더는 상단에 고정되어 긴 라이브·일정 화면에서도 탐색을 유지한다.
- `720px` 이하에서는 브랜드와 사용자 ID를 첫 줄에, 가로 스크롤 메뉴를 둘째 줄에 배치한다. 메뉴를 축약하거나 숨기지 않는다.

### 관리자

- 데스크톱에서는 폭 `244px`의 왼쪽 사이드바를 사용한다. 브랜드, 운영 메뉴, 관리자 ID와 사용자 화면 복귀 링크를 세로로 분리한다.
- 활성 메뉴는 금색 텍스트와 낮은 채도의 금색 배경으로 표시한다.
- `860px` 이하에서는 사이드바를 상단의 가로형 운영 헤더로 전환하고 메뉴는 가로 스크롤을 허용한다.
- `560px` 이하에서는 브랜드와 관리자 ID를 첫 줄에, 운영 메뉴를 다음 줄에 둔다. 페이지 내용은 좌우 `16px` 여백을 유지한다.

## 색상 토큰

실제 토큰은 `src/shared/styles/tokens.css`에서 관리한다.

| 역할 | 토큰 | 값 | 사용 |
|---|---|---|---|
| 앱 배경 | `--app-bg` | `#0E1013` | 사용자 기본 배경 |
| 상승 배경 | `--app-bg-raised` | `#12161C` | 패널, 입력, 헤더 |
| 보조 표면 | `--app-bg-muted` | `#171B22` | 선택·호버 보조 |
| 경계선 | `--app-line` | `#262C35` | 컨트롤과 주요 구획 |
| 약한 경계선 | `--app-line-soft` | `#1A1F26` | 표 행과 내부 구획 |
| 본문 | `--app-text` | `#EDEFF2` | 제목과 핵심 값 |
| 보조 본문 | `--app-text-muted` | `#AEB5C2` | 설명과 표 데이터 |
| 계측 라벨 | `--app-text-faint` | `#747C8B` | 필드 라벨과 메타 정보 |
| UI 블루 | `--app-blue` | `#5AA9E6` | 포커스, 활성선, 블루팀 |
| 팀 레드 | `--app-red` | `#E4514C` | 레드팀, 위험·오류 |
| 리워드 금색 | `--app-gold` | `#D9A441` | 포인트, 쿠폰, 주요 CTA |
| 성공 초록 | `--app-green` | `#55C98A` | 성공·실행 중 상태 |
| 포커스 | `--app-focus` | `#7CC3F5` | 키보드 포커스 링 |

새 기능에서 블루와 레드를 일반 CTA 색으로 혼용하지 않는다. 위험 작업은 레드 윤곽 버튼, 일반 보조 작업은 중립 윤곽 버튼을 사용한다.

## 타이포그래피와 숫자

- 기본 서체는 로컬 `SUIT Variable`이며, `IBM Plex Sans`, 시스템 산세리프 순으로 대체한다.
- 제목은 `800–900`, 본문과 데이터는 `500–750` 범위로 위계를 만든다.
- 페이지 제목은 `clamp(1.9rem, 3vw, 2.7rem)`, 섹션 제목은 약 `1.18rem`, 표 헤더와 계측 라벨은 `0.66–0.78rem`을 기준으로 한다.
- 숫자는 `tabular-nums`를 사용해 점수, 포인트, 수량과 진행률이 갱신될 때 열이 흔들리지 않게 한다.
- 관리자 화면에서 영문 대문자와 넓은 자간은 `ADMIN`, ID, 표 헤더 같은 짧은 계측 라벨에만 사용한다.

## 간격, 밀도와 형태

- 기본 단위는 `4px`이며 주요 간격은 `8`, `12`, `16`, `18`, `22`, `24`, `28`, `32px`를 사용한다.
- 페이지 좌우 여백은 데스크톱 `24–58px`, 모바일 `16px`다. 콘텐츠 최대 폭은 사용자 `1440px`, 관리자 `1480px`다.
- 데이터 표면 내부 여백은 데스크톱 `24px`, 모바일 `18px`를 기본으로 한다.
- 일반 모서리는 `6px`, 입력과 버튼은 `4px`다. 캡슐형 버튼과 과도하게 둥근 카드는 사용하지 않는다.
- 주요 컨트롤은 최소 높이 `44px`를 확보한다. 작은 보조 버튼만 `36px`를 허용한다.
- 표면 깊이는 1px 경계와 배경 명도 차로 표현한다. `--app-shadow`는 겹침 계층이 필요한 경우에만 제한적으로 사용한다.

## 구성 요소와 상태

### 제목과 표면

- `PageHeader`는 제목, 한 줄 설명과 우측 핵심 작업을 조립한다. 모바일에서는 작업을 제목 아래로 내린다.
- `data-surface`는 도메인에 독립적인 기본 표면이다. 화면마다 중첩 카드를 반복하지 않고 실제 정보 구획에만 사용한다.
- 관리자 요약 수치는 개별 카드보다 하나의 분할된 계측 영역으로 보여 준다.

### 버튼과 입력

- `button-primary`: 금색 배경의 한 화면 핵심 작업.
- `button-secondary`: 차콜 배경과 중립 윤곽의 보조 작업.
- `button-danger`: 레드 윤곽과 낮은 채도의 레드 배경을 사용하는 취소·삭제 작업.
- 모든 필드에는 눈에 보이는 라벨을 두고, 단위는 입력 오른쪽에 분리한다. 포커스 시 블루 경계와 링을 함께 표시한다.
- 비활성 상태는 투명도를 낮추고 `not-allowed` 커서를 제공한다.

### 표, 배지와 진행 상태

- 표는 헤어라인으로 행을 구분하고 긴 데이터는 전용 가로 스크롤 컨테이너 안에 둔다.
- `StatusBadge`는 상태 문자열과 색상을 함께 사용한다. 성공·발급·승리는 초록, 준비·진행은 블루, 취소·실패는 레드, 종료·사용 완료는 중립색이다.
- 진행률은 두꺼운 카드 장식 대신 얇은 금색 진행선, 백분율과 처리 건수를 함께 표시한다.

### 비동기 상태

- 로딩, 빈 결과와 오류는 기존 레이아웃 안에서 `AsyncState`로 표시하며 페이지 전체를 대체하지 않는다.
- 로딩은 짧은 명도 펄스만 사용하고, 오류는 레드 경계와 설명 문구를 제공한다.
- API 오류를 숨기지 않되 빈 결과와 오류를 같은 상태처럼 표현하지 않는다.

## 반응형 원칙

- `1180px` 이하에서는 관리자 수치 영역과 쿠폰 항목 폼을 2열로 줄인다.
- `760px` 이하에서는 관리자 수치, 폼, 상세 정의 목록과 빠른 작업을 1열로 전환한다.
- 표와 중계 데이터는 모바일에서 의미 있는 열을 임의로 삭제하지 않고 가로 스크롤 또는 기존 컴포넌트의 축약 규칙을 사용한다.
- 모바일 폼 컨트롤은 최소 `16px` 글자 크기를 유지해 브라우저 자동 확대를 방지한다.
- 고정 헤더나 메뉴가 콘텐츠를 가리지 않아야 하며, 최소 지원 폭은 `320px`다.

## 접근성과 동작

- 사용자와 관리자 셸 모두 본문 바로가기 링크와 명시적인 `main` 진입점을 제공한다.
- 모든 키보드 조작 요소는 `2px` 블루 포커스 링을 표시한다.
- 주요 터치 대상은 `44px` 이상이며, 상태는 색상과 텍스트를 함께 사용한다.
- 로딩은 `role="status"`, 오류는 `role="alert"`, 결과 갱신은 필요한 곳에 `aria-live`를 사용한다.
- 애니메이션은 상태 변화와 방향 이해에 필요한 `160–220ms` 수준으로 제한한다. `prefers-reduced-motion`에서는 펄스, 전환과 기존 패널 진입 동작을 사실상 제거한다.
- 본문과 컨트롤은 WCAG AA 수준의 명도 대비를 목표로 한다.

## 보호되는 기존 화면

- `src/components/MainScreen.jsx`와 `src/mainScreen.css`는 기존 메인 화면의 시각 기준이며 별도 이슈 없이 재설계하지 않는다.
- `src/components/DebugPanel.jsx`와 공개 `/diagnostics` 메뉴의 마크업, 동작과 노출은 별도 요청 없이 수정하거나 이동하지 않는다.
- 보호 화면은 새 사용자 앱 셸로 감쌀 수 있지만 내부 표현과 동작은 그대로 유지한다.
- 기존 컴포넌트가 사용하는 IBM Plex Sans, 2px 모서리, 크롭마크와 계측 그리드는 호환을 위해 유지한다. 새 화면은 SUIT 기반 공통 토큰을 따르며 대규모 기계적 이동이나 스타일 일괄 치환을 하지 않는다.

## 구현 기준

- 공통 토큰과 셸 스타일은 `src/shared/styles`에서 관리한다.
- 새 페이지는 `pages`, 도메인 UI는 `features`, 공통 상태와 표시 요소는 `shared/components`에 둔다.
- 사용자 기능과 관리자 기능은 같은 색상·타이포그래피 체계를 공유하되 탐색 구조를 섞지 않는다.
- 배팅 문구와 선택 대상은 매치 최종 승자가 아니라 현재 세트 승리 팀을 기준으로 한다.
